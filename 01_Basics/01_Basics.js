"Use Strict"; // treat all JS Code as newer version.
// code readability should be high

// * Variables 

let number = 5;
let String = "Kaif ansari";
let BigInt = 565545;
let boolean = 5 == 5;
let Null = null;                   // This is an object, It's not Undefined.
let UnDefined;
let Symbol = '@';

// Three ways to define a variable
// let, const, var ('var doesn't work well with the lastest fetures').

// * typeof                       // Used to check the datatype of a variable.
let name = "Kaif Ansari";
console.log(typeof(name));
console.log(typeof name);